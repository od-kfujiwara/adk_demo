[project]
name = "adk-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-adk[eval,web]>=1.12.0",
    "psycopg2-binary>=2.9.0",  # PostgreSQLアダプタ（ADKのDatabaseSessionService用）
    "python-dotenv>=1.0.0",     # .envファイルから環境変数を読み込むため
]
